{
  "name": "@cgp/relay-cloudflare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "deploy:dry": "npx wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "types": "npx wrangler types src/worker-configuration.d.ts"
  },
  "dependencies": {
    "@cgp/core": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240101.0"
  }
}
